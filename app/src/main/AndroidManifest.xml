<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.naveen.itfest">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplashActivity"
            android:theme="@style/splash"></activity>
        <activity
            android:name=".LoginActivity"
            android:label="LOGIN" />
        <activity
            android:name=".MainActivity"
            android:label="Events"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".RegisterActivity"
            android:label="Register" />
        <activity android:name=".PostCompetitionsActivity" />
        <activity android:name=".PostWinnersActivity" />
        <activity android:name=".CompetitionsSingleActivity" />
        <activity android:name=".WinnersSingleActivity" />
        <activity android:name=".ITQuizTestActivity" />
        <activity android:name=".TreasureTestActivity" />
        <activity android:name=".CodingTestActivity" />
        <activity android:name=".CoordinatorActivity" />
        <activity android:name=".WelcomeActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar">>
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>